<div class="container">
    <div class="row my-4">
      <div class='mx-auto col-6'>
        <h2>Log in</h2>

        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="col-10 my-4">
            <%= f.label :email %><br />
            <%= f.email_field :email, :size => "50", autofocus: true, autocomplete: "email" %>
          </div>

          <div class="col-10 mt-4">
            <%= f.label :password %><br />
            <%= f.password_field :password, :size => "50", autocomplete: "current-password" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="col-10">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>

          <div class="col">
            <%= f.submit "Log in", class: "btn btn-info my-4 sign_in" %>
          </div>
        <% end %>

          <div class="col my-4">
            <P>会員登録がお済みでない場合は、
              <span><%= link_to "新規登録", new_member_registration_path %></span>
              をお願いします。
            </P>
          </div>

    </div>
  </div>
</div>